import { redirect } from "next/navigation";

// Moved under the Integrations area alongside the rest of Connections.
export default function ConnectionsSetupMovedPage() {
  redirect("/integrations/connections/setup");
}
